package com.isep.rpg;

public class Healer extends SpellCaster{
    public Healer(int manaPoints, int lifePoints, int armor, int weaponDamage) {
        super(manaPoints, lifePoints, armor, weaponDamage);
    }
}
